function soma (x, y) {
   const resultado = x + y;
   return resultado
}

const resultado = soma('Lucas', ' Ribeiro');
console.log(resultado);

const raiz = function(n) {
    return n ** 0.5;
}

console.log(raiz(9));
